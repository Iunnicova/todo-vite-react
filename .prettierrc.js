{
  // !"semi": true,
  // Добавлять точку с запятой в конце каждой строки (true = да, false = нет)
  // Рекомендуется true, чтобы избежать ошибок в некоторых случаях
  //! "trailingComma": "es5",
  // Добавлять запятые в конце объектов, массивов и функций
  // "es5" — добавляет запятые там, где это разрешено стандартом ES5 (в объектах, массивах, но не в параметрах функций)
  // Варианты: "none", "es5", "all"
  //! "singleQuote": true,
  // Использовать одинарные кавычки (' ') вместо двойных (" ")
  // true = одинарные, false = двойные
  //! "printWidth": 100,
  // Максимальная длина строки в символах
  // Если строка длиннее 100 символов — Prettier перенесёт её на новую строку
  // Популярные значения: 80, 100, 120
  //! "tabWidth": 2,
  // Количество пробелов, которое считается одним отступом (табуляцией)
  // Обычно используют 2 пробела в React-проектах
  //! "useTabs": false,
  // Использовать табуляцию (символ \t) вместо пробелов
  // false = использовать пробелы (рекомендуется)
  //! "bracketSpacing": true,
  // Добавлять пробелы внутри фигурных скобок объектов
  // true  → { foo: bar }
  // false → {foo: bar}
  //! "arrowParens": "always",
  // Добавлять скобки у стрелочных функций
  // "always"  → (x) => x
  // "avoid"   → x => x
  // Рекомендуется "always" для ясности
  //! "endOfLine": "lf",
  // Какой символ конца строки использовать
  // "lf"   → Unix / Linux / macOS (рекомендуется)
  // "crlf" → Windows
  // "auto" → автоматически определять
  //! "jsxSingleQuote": false,
  // Использовать одинарные кавычки в JSX (в атрибутах)
  // false = использовать двойные кавычки в JSX (" ")
  // true  = использовать одинарные (' ')
  // !
  // Где ставить закрывающую скобку в многострочных JSX-элементах
  // false →
  //   <div
  //     className="..."
  //   >
  // true →
  //   <div
  //     className="..."
  //     >
  //! "quoteProps": "as-needed"
  // Когда ставить кавычки у ключей в объектах
  // "as-needed" → ставить только когда необходимо (например, если ключ содержит пробел или спецсимвол)
  // "consistent" → всегда одинаково
  // "preserve" → оставлять как есть
}

//! ### Отформатировать все файлы вручную
// npx prettier --write .

//! ### Проверить без изменения:
// npx prettier --check .
